#pragma once
#include <algorithm>
template<class T> void quickSort(T* tab, int left, int right);
template<class T> void mergeSort(T* tab, int left, int right);
template<class T> void introSort(T* tab, T* Left, T* Right, int maxdepth);